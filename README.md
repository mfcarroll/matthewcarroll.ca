# matthewcarroll.ca

Personal landing page. Plain HTML/CSS with a small theme-toggle script — no
build step, no dependencies.

**Run locally:** `python3 -m http.server` and open <http://localhost:8000>.

**Deploy:** pushing to `main` publishes to GitHub Pages via
`.github/workflows/deploy.yml`. The custom domain is set by `CNAME`.
